%h1 Pets
%hr

= link_to "New Pet", new_pet_path, class: "btn btn-success"

%table.table.table-striped
	%thead
		%th Name
		%th
	%tbody
		- @pets.each do |pet|
			%tr
				%td= pet.name
				%td= link_to "Edit", edit_pet_path(pet), class: "btn btn-success"

= paginate @pets